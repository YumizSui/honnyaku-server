type: runtime

startup: |
  git submodule update --init --recursive
  cd ./client
  npm ci
  npm run build
  cd ../
  go mod download
  go build -o main

entrypoint: ./main

http_proxy: 4000

branch:
  master: null